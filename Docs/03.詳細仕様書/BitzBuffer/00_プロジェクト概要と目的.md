# BitzBuffer 設計仕様 - BitzBuffer概要とアーキテクチャ

このドキュメントは、C# Buffer管理ライブラリ「BitzBuffer」のコア機能に関するアーキテクチャ概要と、この詳細設計仕様書群の構成について説明します。

ライブラリ全体の目的とスコープについては、システム仕様書 [`../../01.システム仕様書/00_プロジェクト概要と目的.md`](../../01.システム仕様書/00_プロジェクト概要と目的.md) を参照してください。

## 1. アーキテクチャ概要 (BitzBufferコア)

BitzBufferコアライブラリは、以下の主要なコンポーネントで構成されます。

*   **`BufferManager`**: アプリケーション全体で `IBufferProvider` を登録・管理し、利用者に提供します。Fluent APIによる設定が可能です。デフォルトプロバイダ（マネージド/アンマネージド）を内包します。
    *   詳細は [プロバイダと実装クラス (`02_プロバイダと実装クラス.md`)](./02_プロバイダと実装クラス.md) を参照してください。
*   **`IBufferProvider`**: 特定の技術領域（マネージド、アンマネージド、GPU等）のバッファに関する操作（プールからの貸し出し、直接生成）を提供します。プロバイダ固有の設定を持ちます。
    *   詳細は [プロバイダと実装クラス (`02_プロバイダと実装クラス.md`)](./02_プロバイダと実装クラス.md) を参照してください。
*   **`IBufferFactory<TItem, TBuffer, TOptions>`**: `IBufferProvider` の内部で使用され、実際にバッファオブジェクト (`IBuffer<TItem>` の実装クラス) をインスタンス化します。基本的な方針は、`Create(TOptions options)` メソッドを持ち、`TOptions` で生成に必要な全情報を受け取ります。
    *   詳細は [プロバイダと実装クラス (`02_プロバイダと実装クラス.md`)](./02_プロバイダと実装クラス.md) での `IBufferProvider` との連携部分を参照してください。
*   **`IBufferPool<TBuffer>` / `IBufferPoolStrategy<TBuffer, TResource, TItem>`**: バッファのプーリング機構です。戦略はカスタマイズ可能です。`ArrayPool<T>` のコンセプトを参考に、独自実装を行います。
    *   詳細は [プーリング (`03_プーリング.md`)](./03_プーリング.md) を参照してください。
*   **`IBufferLifecycleHooks<TBuffer, TItem>`**: プールされるバッファのライフサイクルイベント（取得時、返却時、クリア処理など）を処理します。
    *   詳細は [プーリング (`03_プーリング.md`)](./03_プーリング.md) を参照してください。
*   **`IBuffer<T>` (および関連インターフェース)**: ライブラリの中心となるバッファインターフェースです。非連続メモリ、所有権管理、ライフサイクルを考慮して設計されています。
    *   詳細は [コアインターフェース (`01_コアインターフェース.md`)](./01_コアインターフェース.md) を参照してください。

## 2. ドキュメント構成 (BitzBufferコア詳細設計)

このBitzBufferコア機能の詳細設計仕様書は、以下のファイルで構成されています。(全てこの `Docs/03.詳細仕様書/BitzBuffer/` フォルダ内に配置)

*   **[`00_BitzBuffer概要とアーキテクチャ.md`](./00_BitzBuffer概要とアーキテクチャ.md) (このファイル):** BitzBufferコアのアーキテクチャ概要と、この詳細設計仕様書群の構成を示します。
*   **[`01_コアインターフェース.md`](./01_コアインターフェース.md):** `IBufferState`, `IOwnedResource`, `IReadOnlyBuffer<T>`, `IWritableBuffer<T>`, `IBuffer<T>` など、中核となるインターフェースの定義、設計思想、セマンティクスについて詳述します。
*   **[`02_プロバイダと実装クラス.md`](./02_プロバイダと実装クラス.md):** `ManagedBuffer<T>`, `NativeBuffer<T>` などの実装クラスの実装詳細と、`BufferManager`, `IBufferProvider` を介したバッファの取得・設定方法について記述します。
*   **[`03_プーリング.md`](./03_プーリング.md):** バッファプーリング戦略、ライフサイクルフック、クリア処理ポリシーなど、メモリ効率化のための機構について詳述します。
*   **[`04_GPUサポート.md`](./04_GPUサポート.md):** 拡張アーキテクチャを通じたGPUバッファサポートの実現方針について記述します。
*   **[`05_エラーハンドリング.md`](./05_エラーハンドリング.md):** BitzBufferコアライブラリ全体のエラーハンドリング戦略、使用する例外の種類（標準・カスタム）、`Try...` パターンの適用箇所について記述します。

## 3. 将来のライブラリ全体の拡張方針 (BitzBufferコア関連)

BitzBuffer コアは、初期リリース後も継続的な改善と機能拡張を目指します。具体的な機能拡張は各コンポーネントの設計仕様書に記載されていますが、ライブラリ全体として以下のような方向性を検討しています。

*   **設定の柔軟性向上:** 設定ファイルからの構成読み込みサポートなど、より多様な環境での利用を容易にします。 (詳細は [`02_プロバイダと実装クラス.md`](./02_プロバイダと実装クラス.md) の将来の拡張を参照)
*   **デバッグと診断機能の統合的強化:** イベントトレースの統合など、ライブラリ全体の動作状況を把握しやすくする機能を追加します。 (詳細は [`05_エラーハンドリング.md`](./05_エラーハンドリング.md) の将来の拡張を参照)
*   **コミュニティとの連携:** 利用者からのフィードバックを積極的に取り入れ、より実践的なユースケースに対応できるよう進化していきます。
