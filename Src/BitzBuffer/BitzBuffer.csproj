<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net6.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- テストプロジェクトから internal メンバーへアクセスできるようにする -->
  <ItemGroup>
    <InternalsVisibleTo Include="BitzBuffer.Tests" />
  </ItemGroup>

</Project>
